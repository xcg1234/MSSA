namespace SQL;

internal class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Hello, World!");
        //add models
        //add dbcontext
        //Add-Migration name(InitialCreate)
        //Update-Database
        //Do some changes to the model
        //Add-Migration name(AddGenre)
        //Update-Database
    }
}